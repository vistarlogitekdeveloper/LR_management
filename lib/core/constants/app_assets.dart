class AppAssets {
  AppAssets._();

  // Tightly-cropped wordmark (the source PNG had ~40% empty margin which made
  // it render tiny). Trimmed to the content bounds so it shows large.
  static const logo = 'assets/images/vistar_logo_trimmed.png';
}
